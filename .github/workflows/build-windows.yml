---
name: Build - Windows

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  job:
    name: ${{ matrix.os }}-${{ matrix.buildtype }}
    timeout-minutes: 15
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-2019]
        buildtype: [windows-release, windows-release-asan, windows-debug]
        include:
          - os: windows-2019
            triplet: x64-windows
            packages: >
              sccache

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: CCache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          max-size: "1G"
          variant: "sccache"
          key: ccache-${{ matrix.os }}-${{ matrix.buildtype }}
          restore-keys: |
            ccache-${{ matrix.os }}

      - name: Get Python 3.10
        uses: actions/setup-python@v1
        with:
          python-version: '3.10'

      - name: Get latest Conan
        run: pip install conan

      - name: Get latest CMake and ninja
        uses: lukka/get-cmake@latest

      - name: Run Conan Install
        run: |
          mkdir build
          cd build
          mkdir ${{ matrix.buildtype }}
          cd ${{ matrix.buildtype }}
          conan install -s compiler='Visual Studio' ../../ --build=missing

      - name: Run CMake Release
        if: ${{ matrix.buildtype == 'windows-release' }}
        run: |
          cd build
          cd ${{ matrix.buildtype }}
          cmake .. -DDEBUG_LOG=OFF -DASAN_ENABLED=OFF -DCMAKE_BUILD_TYPE=Release
          cmake --build . --config Release

      - name: Run CMake Release Asan
        if: ${{ matrix.buildtype == 'windows-release-asan' }}
        run: |
          cd build
          cd ${{ matrix.buildtype }}
          cmake .. -DDEBUG_LOG=OFF -DASAN_ENABLED=ON -DCMAKE_BUILD_TYPE=Release
          cmake --build . --config Release

      - name: Run CMake Debug
        if: ${{ matrix.buildtype == 'windows-debug' }}
        run: |
          cd build
          cd ${{ matrix.buildtype }}
          cmake .. -DDEBUG_LOG=ON -DASAN_ENABLED=ON -DCMAKE_BUILD_TYPE=Debug
          cmake --build . --config Debug

      - name: Create and Upload Artifact (exe)
        uses: actions/upload-artifact@v3
        with:
          name: otclient-${{ matrix.buildtype }}-${{ github.sha }}
          path: |
            ${{ github.workspace }}/otclient.exe

      - name: Create and Upload Artifact (dlls)
        uses: actions/upload-artifact@v3
        with:
          name: otclient-${{ matrix.buildtype }}-${{ github.sha }}
          path: |
            ${{ github.workspace }}/*.dll
